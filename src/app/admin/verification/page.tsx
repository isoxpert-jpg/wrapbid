import { Notice } from '@/components/Notice'
import { prisma } from '@/lib/db'
export default async function Verification({searchParams}:{searchParams:Promise<{ok?:string;error?:string}>}){const [vehicles,brands]=await Promise.all([prisma.vehicle.findMany({where:{verificationStatus:{not:'VERIFIED'}},include:{driver:true}}),prisma.brand.findMany({where:{status:{not:'APPROVED'}},include:{owner:true}})]);return <div className="flex flex-col gap-5"><h1 className="text-2xl font-bold">Verification queue</h1><Notice {...await searchParams}/><section><h2 className="font-bold">Vehicles</h2>{vehicles.map(v=><form className="card mt-3 grid gap-3 p-4 sm:grid-cols-4" key={v.id} action="/api/workflow/verify-vehicle" method="post"><input type="hidden" name="vehicleId" value={v.id}/><div><strong>{v.year} {v.make} {v.model}</strong><p className="text-sm muted">{v.driver.name} · plate {v.plateNumber}</p></div><select className="select" name="status"><option>VERIFIED</option><option>REJECTED</option><option>PENDING_REVIEW</option></select><input className="input" name="conditionRating" type="number" min="1" max="5" placeholder="Condition 1–5"/><button className="btn btn-primary">Save review</button></form>)}{vehicles.length===0&&<p className="mt-2 text-sm muted">Queue clear.</p>}</section><section><h2 className="font-bold">Brands</h2>{brands.map(b=><form className="card mt-3 grid gap-3 p-4 sm:grid-cols-3" key={b.id} action="/api/workflow/review-brand" method="post"><input type="hidden" name="brandId" value={b.id}/><div><strong>{b.name}</strong><p className="text-sm muted">{b.owner.name} · {b.industry}</p></div><select className="select" name="status"><option>APPROVED</option><option>REJECTED</option><option>PENDING_REVIEW</option></select><button className="btn btn-primary">Save review</button></form>)}{brands.length===0&&<p className="mt-2 text-sm muted">Queue clear.</p>}</section></div>}
