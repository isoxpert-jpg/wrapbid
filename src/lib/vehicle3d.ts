export function uniformScaleForLength(boundsLength:number,targetLengthMeters:number){if(!Number.isFinite(boundsLength)||boundsLength<=0||!Number.isFinite(targetLengthMeters)||targetLengthMeters<=0)return null;return targetLengthMeters/boundsLength}
export function artworkDpi(widthPx:number,heightPx:number,widthCm:number,heightCm:number){const widthDpi=widthPx/(widthCm/2.54);const heightDpi=heightPx/(heightCm/2.54);return{widthDpi:Math.round(widthDpi),heightDpi:Math.round(heightDpi),effectiveDpi:Math.round(Math.min(widthDpi,heightDpi)),recommendedWidthPx:Math.ceil(100*widthCm/2.54),recommendedHeightPx:Math.ceil(100*heightCm/2.54)}}
